import { AdminAuthorizationDirective } from './admin-authorization.directive';

describe('AdminAuthorizationDirective', () => {
  it('should create an instance', () => {
    const directive = new AdminAuthorizationDirective();
    expect(directive).toBeTruthy();
  });
});
